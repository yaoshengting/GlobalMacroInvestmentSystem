# Portfolio Data

Only sanitized examples belong in Git. `actual_portfolio.yaml` is deliberately ignored and must remain local. The system generates human-reviewed analysis only and never places orders.

