# Hypothesis Registry

Canonical machine-readable hypotheses live in `hypotheses.yaml`. Review changes against primary evidence and append updates to the matching knowledge file; do not rewrite history.

